# AxonHubClient 项目看板

最后更新：2026-06-11

本文档只记录任务推进状态。产品定位、能力边界和安全模型见 `docs/产品设计.md`；完整命令和参数见 `docs/使用说明.md`。

## 使用规则

- 新任务先放入 `待办`。
- 开始处理时移入 `进行中`。
- 遇到外部依赖或信息不足时移入 `阻塞`。
- 完成后移入 `已完成`，只保留简短结果说明。
- 每次更新只移动条目，不重复写长说明。
- 待办按优先级分为 `P0` / `P1` / `P2`，同一优先级内可按依赖顺序推进。

## 待办

### P2：组织和系统管理

- 项目只读盘点：`projects list|get`、项目 API Key / 用量概览
- 用户和角色只读盘点：`users list|get`、`roles list|get`、权限 scope 展示
- 项目、用户、角色写操作：创建、更新、启停、项目成员关系管理，默认 dry-run
- 系统配置只读盘点：品牌、存储、重试、Webhook、模型设置、Quota、安全设置
- 高影响系统操作设计：backup、restore、cache cleanup、GC cleanup，只先做方案和风险分级

### P2：工具化与 Agent 配套能力

- Agent / Skill 使用说明：让 Agent 优先走 `AxonHubClient` CLI / Python client，浏览器只做辅助验证

## 进行中

- 无

## 阻塞

### 待确认

- 是否把路由与成本优化作为近期重点：`routing inspect`、`routing suggest --goal lowest-cost|balanced`
- 是否开放 API Key / Profile 写操作：创建、更新、启停、归档、Profile 绑定
- 是否研究并开放 API Key rotate / 删除能力
- 是否需要 Thread / execution 只读命令，或先只做 requests 与 traces
- 是否实现 OpenAPI service account 子客户端，还是继续只聚焦 Admin GraphQL

## 已完成

- 包发布准备：统一用户可见命名为 `axonhub-client`，补齐 GitHub 手动发布、PyPI 发布和 Windows x64 便携 zip workflow
- 基础 client 与 CLI：建立 Admin Token 模式、REST 状态接口、Admin GraphQL transport 和 `axonhub-client` 命令入口
- Session 全局化：默认 session 迁移到用户配置目录，支持登录、登出、当前用户和失效提示
- 渠道管理：补齐只读盘点、创建、批量创建、导入、更新、启停、排序、endpoint 保存、测试、模型同步、归档、恢复和删除
- 渠道内 API key 管理：补齐测试、禁用、启用、启用全部、启用选中和清理 disabled key 记录，保持默认 dry-run 和脱敏输出
- 模型管理：补齐只读盘点、创建、批量创建、更新、启停、归档、恢复、删除、最快模型和关联规则管理
- API Key / Profile 盘点：补齐列表、详情、配额用量和 Profile 模板读取，不选择明文 `key`
- 运维排障：补齐 `requests`、`usage logs`、`traces` 只读命令、渠道健康诊断、inventory summary 和只读 smoke test
- CLI 接口收敛：统一命名、筛选参数、dry-run 输出、`--confirm` 确认方式和敏感字段脱敏
- 文档与示例：补齐中文使用说明、产品设计、项目看板、README 和 `docs/examples/` 示例输入
- 工程治理：加入 `pytest` 开发依赖，拆分超过 800 行风险的 client、CLI、GraphQL query 和测试模块
- 真实实例验证：完成登录、核心只读查询、dry-run 写命令、smoke test、模型关联规则和 iKunCode 渠道相关验证

